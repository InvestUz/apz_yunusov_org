# ART Monitoring System — Contract & Fakt Integration

Комплексная система мониторинга договоров ART (Архитектура-Планировка-Топшириги) с интеграцией контрактов и фактических платежей.

## 🎯 Возможности системы

### ✅ Реализованные функции

1. **База данных**
   - ✅ 3 миграции: contracts, payments, payment_schedules
   - ✅ Полные связи между таблицами
   - ✅ Индексы для оптимизации запросов

2. **Backend**
   - ✅ Парсинг CSV файлов (contracts.csv, fakt.csv)
   - ✅ Автоматическое сопоставление платежей с контрактами по INN/PINFL/Passport
   - ✅ Интеллектуальное разрешение множественных контрактов
   - ✅ Генерация графиков платежей
   - ✅ Расчет задолженностей и просроченных сумм
   - ✅ API endpoints для данных dashboard

3. **Frontend (Laravel Blade + CDN)**
   - ✅ Адаптивная панель мониторинга
   - ✅ Bootstrap 5 для UI
   - ✅ Chart.js для графиков
   - ✅ Alpine.js для интерактивности
   - ✅ Цветовая схема: primary: #0057B8, success: #2ECC71, warning: #F1C40F, danger: #E74C3C

4. **Вычисления и статистика**
   - ✅ Общее количество и сумма контрактов
   - ✅ Активные, отмененные, завершенные контракты
   - ✅ Таблица мониторинга по районам
   - ✅ План — Факт — Задолженность по кварталам (2024-2027)
   - ✅ Просроченные платежи
   - ✅ Все суммы в миллиардах сум

## 📦 Установка и запуск

### Предварительные требования
- PHP >= 8.1
- Composer
- MySQL/PostgreSQL/SQLite
- CSV файлы в `public/dataset/`

### Шаг 1: Установка зависимостей
```bash
composer install
```

### Шаг 2: Настройка окружения
```bash
cp .env.example .env
php artisan key:generate
```

Настройте подключение к базе данных в файле `.env`:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=art_monitoring
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

### Шаг 3: Миграции и заполнение данных
```bash
# Создание таблиц
php artisan migrate

# Заполнение данных из CSV (seeding)
php artisan db:seed
```

**ВАЖНО:** Команда `php artisan db:seed` автоматически:
1. Парсит contracts.csv
2. Парсит fakt.csv
3. Сопоставляет платежи с контрактами
4. Генерирует графики платежей
5. Рассчитывает задолженности

### Шаг 4: Запуск сервера
```bash
php artisan serve
```

Откройте браузер: http://localhost:8000/dashboard

## 📊 Структура базы данных

### Таблица: contracts
| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | Primary key |
| contract_number | string | Номер договора (APT-X/24) |
| inn | string | ИНН |
| pinfl | string | ПИНФЛ |
| passport | string | Серия паспорта |
| company_name | string | Название компании |
| district | string | Район |
| status | enum | Статус: Амалдаги, Бекор қилинган, Якунланган |
| contract_amount | decimal(20,2) | Сумма договора |
| initial_payment | decimal(20,2) | Начальный платеж |
| remaining_amount | decimal(20,2) | Остаток |

### Таблица: payments
| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | Primary key |
| contract_id | bigint | FK -> contracts |
| payment_date | date | Дата платежа |
| inn | string | ИНН плательщика |
| amount_debit | decimal(20,2) | Сумма прихода |
| amount_credit | decimal(20,2) | Сумма возврата (отрицательная) |
| is_matched | boolean | Сопоставлен ли с контрактом |

### Таблица: payment_schedules
| Поле | Тип | Описание |
|------|-----|----------|
| id | bigint | Primary key |
| contract_id | bigint | FK -> contracts |
| year | int | Год |
| quarter | int | Квартал (1-4) |
| period | string | "2025 Q3", "2026" и т.д. |
| planned_amount | decimal(20,2) | Плановая сумма |
| actual_amount | decimal(20,2) | Фактическая сумма |
| debt_amount | decimal(20,2) | Задолженность |
| is_overdue | boolean | Просрочен ли |

## 🔧 Основные компоненты

### Services

**ContractService** (`app/Services/ContractService.php`)
- Парсинг CSV файлов
- Сопоставление платежей с контрактами
- Генерация графиков платежей
- Расчет задолженностей

**DashboardService** (`app/Services/DashboardService.php`)
- Сводная статистика
- Группировка по районам
- Расчет по периодам
- Форматирование сумм в миллиардах

### Models

- **Contract** - модель договоров с relationships
- **Payment** - модель платежей
- **PaymentSchedule** - модель графиков платежей

### Controllers

**DashboardController** (`app/Http/Controllers/DashboardController.php`)
- `index()` - главная страница dashboard
- `summary()` - API для сводки
- `contracts()` - API для контрактов
- `debts()` - API для задолженностей
- `overdue()` - API для просроченных

## 📈 Функции dashboard

### Карточки статистики
1. Жами шартномалар (Всего договоров)
2. Амалдаги шартномалар (Действующие)
3. Тўлиқ тўланган (Полностью оплаченные)
4. Қарздорлик (Задолженность)

### Графики
1. **Распределение по районам** - гистограмма сумм и задолженностей
2. **План-Факт-Задолженность** - линейный график по кварталам

### Таблица мониторинга
Полная таблица с колонками:
- Т/р (Номер)
- Ҳудуд (Район)
- Жами АРТ шартномалар (сони, млрд)
- Бекор қилинган
- Тўлиқ тўланган
- Амалдаги шартномалар
- 2025 III чорак: Режа / Факт / Қарздорлик
- 2025 IV чорак: Режа / Факт / Қарздорлик
- 2026 йил: Режа / Факт / Қарздорлик
- 2027 йил: Режа / Факт / Қарздорлик

### Таблица просроченных платежей
Отображает контракты с просроченными платежами

## 🎨 Цветовая схема

```css
--primary: #0057B8   /* Синий - основной цвет */
--success: #2ECC71   /* Зеленый - успех */
--warning: #F1C40F   /* Желтый - предупреждение */
--danger:  #E74C3C   /* Красный - опасность */
--info:    #3498DB   /* Голубой - информация */
--secondary: #95A5A6 /* Серый - второстепенный */
```

## 🔍 Алгоритм сопоставления платежей

1. **Поиск по идентификатору** (INN/PINFL/Passport)
2. **Если найден 1 контракт** → автоматическое сопоставление
3. **Если найдено несколько контрактов** → интеллектуальный выбор:
   - Совпадение по дате (платеж >= даты контракта) +3 балла
   - Совпадение по району +2 балла
   - Активный статус +1 балл
   - Выбирается контракт с максимальным баллом

## 📝 Команды Artisan

```bash
# Миграции
php artisan migrate
php artisan migrate:fresh  # Очистка и пересоздание таблиц

# Seeding
php artisan db:seed                      # Запуск всех seeders
php artisan db:seed --class=ARTDataSeeder # Только ART данные

# Полный сброс + seeding
php artisan migrate:fresh --seed

# Очистка кеша
php artisan cache:clear
php artisan config:clear
php artisan route:clear
```

## 🌐 API Endpoints

```
GET /dashboard                 - Главная страница dashboard
GET /api/dashboard/summary     - JSON сводка
GET /api/dashboard/contracts   - JSON контракты по районам
GET /api/dashboard/debts       - JSON задолженности по периодам
GET /api/dashboard/overdue     - JSON просроченные платежи
```

## 📁 Структура проекта

```
app/
├── Http/Controllers/
│   └── DashboardController.php
├── Models/
│   ├── Contract.php
│   ├── Payment.php
│   └── PaymentSchedule.php
└── Services/
    ├── ContractService.php
    └── DashboardService.php

database/
├── migrations/
│   ├── 2024_12_02_000001_create_contracts_table.php
│   ├── 2024_12_02_000002_create_payments_table.php
│   └── 2024_12_02_000003_create_payment_schedules_table.php
└── seeders/
    ├── ARTDataSeeder.php
    └── DatabaseSeeder.php

resources/views/
├── layouts/
│   └── app.blade.php
└── dashboard/
    └── index.blade.php

public/dataset/
├── contracts.csv
└── fakt.csv
```

## 🚀 Особенности реализации

### ✅ Выполненные требования
- ☑️ CSV импорт заменен на seeding
- ☑️ Автоматическое сопоставление по INN/PINFL/Passport
- ☑️ Интеллектуальное разрешение конфликтов при множественных контрактах
- ☑️ Расчет задолженностей по графику
- ☑️ Определение просроченных платежей
- ☑️ Форматирование в миллиардах сум
- ☑️ Полная таблица мониторинга по районам
- ☑️ Интерактивные графики (Chart.js)
- ☑️ Адаптивный дизайн (Bootstrap 5)
- ☑️ Использование только CDN (без npm)

### 🎯 AI-like алгоритмы
- Нормализация идентификаторов (INN/PINFL)
- Парсинг дат с обработкой ошибок
- Парсинг сумм с различными форматами
- Интеллектуальный scoring для выбора контракта
- Автоматическая группировка по периодам

## 📊 Формат данных

### Суммы
Все суммы хранятся в `decimal(20,2)` и отображаются в миллиардах:
```php
{{ number_format($amount / 1000000000, 2) }} млрд сўм
```

### Статусы контрактов
- `Амал қилувчи` - Действующий
- `Бекор қилинган` - Отмененный
- `Якунланган` - Завершенный

## 🔐 Безопасность
- Защита от SQL-инъекций через Eloquent ORM
- Валидация CSV данных при импорте
- Безопасное отображение данных в Blade (автоматический escaping)

## 🐛 Отладка

Если данные не загружаются:
```bash
# Проверьте путь к CSV
ls -la public/dataset/

# Проверьте логи Laravel
tail -f storage/logs/laravel.log

# Пересоздайте базу
php artisan migrate:fresh --seed
```

## 📄 Лицензия
MIT License

## 👨‍💻 Разработано для
ART Monitoring System — Yunusov Organization
Tashkent Invest Company
